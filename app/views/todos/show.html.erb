<h1><%= @todo.title %></h1>
<p><%= @todo.body %></p>
<hr>
<div class="btn-group" role="group" aria-label="Basic example">
  <span class="me-1 py-1"><%= link_to 'Редактировать', edit_todo_path(@todo),
  :class => 'btn btn-warning' %></span>
  <span class="me-1 py-1"><%= button_to 'Удалить', todo_path(@todo),
  method: :delete, :class => 'btn btn-danger' %></span>
</div>
<hr>
<h2>все коментарии</h2>
<%= render 'comments/comment' %>
<hr>
<h2>Добавление коментария</h2>
<%= render 'comments/form' %>